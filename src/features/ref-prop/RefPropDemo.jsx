const RefPropDemo = () => {
  return (
    <div>
        <h1 className="text-2xl font-bold underline">Ref Prop Demo</h1>
        <p>This is a demo of the new Ref Prop API in React 19.2.</p>
        <p>Ref props allow you to pass refs directly to child components, enabling more flexible and reusable component patterns.</p>
        <p>In this demo, we'll show how to create and use actions for common tasks like adding items to a cart, updating user profiles, and more.</p>
    </div>
  );
}   

export default RefPropDemo;